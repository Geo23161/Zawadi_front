import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'zawadi.elifeg.com',
  appName: 'Zawadi',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
